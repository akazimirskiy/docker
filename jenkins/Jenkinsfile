pipeline {
    agent any
    stages {
        stage('Build app') {
            steps {
                sh "jenkins/scripts/build.sh"
            }
        }
        stage('Test app') {
            steps {
                sh "jenkins/scripts/test.sh"
            }
        }
        stage('Deploy app') {
            steps {
                sh "jenkins/scripts/deploy.sh"
            }
        }
    }
}